# Airflow Web Server Settings
AIRFLOW_WEB_SERVER_HOST=0.0.0.0
AIRFLOW_WEB_SERVER_PORT=8080

AIRFLOW__WEBSERVER__WORKERS=2
AIRFLOW__WEBSERVER__WORKER_TIMEOUT=120
AIRFLOW__WEBSERVER__WEB_SERVER_PORT=8080

# SQL_ALCHEMY_CONN will default to sqlite:///$AIRFLOW_HOME/airflow.db if not set
# AIRFLOW__CORE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@localhost:5432/airflow


# --- Redis Pub/Sub Settings ---
REDIS_HOST=localhost
REDIS_PORT=6480
REDIS_CHANNEL=agent_pubsub_queue

# --- Authentication (for API calls / DAG trigger) ---
AUTH_USER=admin
AUTH_PASSWORD=leocdp123

# --- Environment Flags ---
DEV_MODE=true

# Trigger mode: true = REST API, false = CLI
USE_API=true
